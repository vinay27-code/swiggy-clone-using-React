/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx}",   // ✅ Scan all your React files
  ],
  theme: {
    extend: {
      // You can customize fonts, colors, breakpoints, etc. here later
    },
  },
  plugins: [],
};
